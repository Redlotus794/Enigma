# 2026-05-23

## 09:30

- 改动范围：`enigma-ddd/enigma-ddd-core`、`enigma-ddd/enigma-ddd-spring-boot-starter`、`enigma-test-spring-boot-starter`、`docs/dev/convention/java-ddd-convention.md`、`agents.md`、`openspec/config.yaml`、`openspec/changes/add-domain-single-entity-aggregate/`
- 改动摘要：移除 `DomainAggregate` 接口，将聚合从 Java 类型契约调整为包级别一致性边界；`DomainRepository` 改为直接面向聚合根 `DomainRepository<DAR extends DomainAggregateRoot<IdentityType>, IdentityType>`；将 `nextIdentity()` 默认契约从聚合根迁移到领域资源库；同步更新单实体聚合测试、Spring 内存资源库泛型和相关文档描述。
- 验证方式：先执行 `cd enigma-ddd && ./mvnw test`，因单独模块无法解析本地 `enigma-bom:2.0.0-SNAPSHOT` 失败；随后执行 `cd enigma-parent && ./mvnw test -pl ../enigma-ddd/enigma-ddd-core,../enigma-ddd/enigma-ddd-spring-boot-starter -am`，首次在沙箱内受 `~/.m2` 写入和本地端口权限影响失败，沙箱外重跑通过，Reactor `BUILD SUCCESS`。

## 09:32

- 改动范围：`enigma-ddd/enigma-ddd-core/src/main/java/io/github/redlotus794/enigma/ddd/core/exception/`、`DomainRepository.java`、`DomainServiceUtils.java` 及对应测试。
- 改动摘要：将 `DomainEntityNotFoundException` 更名为 `DomainAggregateRootNotFoundException`，使 `DomainRepository.findRequired()` 的未找到异常与聚合根资源库语义一致，并同步更新默认异常消息和单元测试。
- 验证方式：执行 `cd enigma-parent && ./mvnw test -pl ../enigma-ddd/enigma-ddd-core -am`，结果 `BUILD SUCCESS`，`enigma-ddd-core` 共 64 个测试通过。

## 09:44

- 改动范围：`DomainEntity.java`、`DomainAggregateRoot.java`、`DomainSingleEntityAggregate.java`、`InMemoryDomainRepository.java`、`CachedDomainRepository.java`、DDD 核心测试、Spring 资源库测试、`docs/dev/convention/java-ddd-convention.md`、`openspec/changes/add-domain-single-entity-aggregate/`。
- 改动摘要：明确聚合根是领域实体的一种特殊类型，普通实体不再默认具备聚合根契约；`DomainAggregateRoot` 显式继承 `DomainEntity`，并补充其代表聚合对外暴露、管理聚合内部对象、保护聚合内部一致性规则的职责说明；同步调整单实体聚合与资源库泛型边界。
- 验证方式：执行 `cd enigma-parent && ./mvnw test -pl ../enigma-ddd/enigma-ddd-core,../enigma-ddd/enigma-ddd-spring-boot-starter -am`，首次沙箱内受本地随机端口限制失败，沙箱外重跑通过，Reactor `BUILD SUCCESS`。

## 11:23

- 改动范围：`DomainAggregateRootNotFoundException.java`、`DomainServiceUtils.java`、`AggregateRootNotFoundExceptionTest.java`。
- 改动摘要：将 `DomainAggregateRootNotFoundException` 默认无参构造器私有化，避免外部创建缺少聚合根标识的未找到异常；`DomainServiceUtils.findBy()` 改为按查询主键构造异常，测试同步校验无参构造器为 private。
- 验证方式：执行 `cd enigma-parent && ./mvnw test -pl ../enigma-ddd/enigma-ddd-core -am`，结果 `BUILD SUCCESS`，`enigma-ddd-core` 共 68 个测试通过。

## 11:33

- 改动范围：`docs/domain-driven-design/资源库.md`、`docs/domain-driven-design/html/资源库.html`。
- 改动摘要：新增资源库领域设计文档及同步 HTML 预览，补充 Repository 面向聚合根、接口与实现分离、资源库生成聚合根主键、避免为聚合内部实体和值对象单独建库等设计约束。
- 验证方式：执行 `rg -n '资源库|nextIdentity|聚合根|基础设施层' docs/domain-driven-design/资源库.md docs/domain-driven-design/html/资源库.html`，确认 Markdown 与 HTML 均包含关键内容；执行 `ls docs/domain-driven-design/资源库.md docs/domain-driven-design/html/资源库.html`，确认源文档与 HTML 预览文件均存在。

## 11:57

- 改动范围：`AggregateRoot.java`、`SingleEntityAggregate.java`、`TestEntity.java`、`TestSingleEntityAggregate.java`、`Shield.java`、`Bracer.java`。
- 改动摘要：按当前 `SingleEntityAggregate<PKType>` 一参泛型设计修复编译错误；`AggregateRoot` 重新显式继承 `Entity`，`SingleEntityAggregate` 只继承 `AggregateRoot<PKType>`，测试实体和示例聚合根移除旧的两参泛型用法。
- 验证方式：执行 `cd enigma-parent && ./mvnw test -pl ../enigma-ddd/enigma-ddd-core -am`，结果 `BUILD SUCCESS`，`enigma-ddd-core` 共 68 个测试通过；随后执行 `cd enigma-parent && ./mvnw test -pl ../enigma-ddd/enigma-ddd-spring-boot-starter,../enigma-test-spring-boot-starter -am`，首次沙箱内受 Spring Boot 随机端口限制失败，沙箱外重跑通过，Reactor `BUILD SUCCESS`。
