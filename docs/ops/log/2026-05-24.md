## 10:33

- 改动范围：`enigma-ddd` 聚合根契约及测试示例、`enigma-demo` 与 `enigma-test-spring-boot-starter` 示例聚合根、`docs/dev/convention/java-ddd-convention.md`、`docs/domain-driven-design/`、`openspec/changes/add-domain-single-entity-aggregate/`。
- 改动摘要：移除独立的单实体聚合概念与 `SingleEntityAggregate` 契约；删除对应 OpenSpec 变更、领域设计 Markdown/HTML 和专用测试类；当聚合边界内只有一个实体时，统一表达为该实体直接实现 `AggregateRoot<IdentityType>`。
- 验证方式：执行 `rg -n "SingleEntityAggregate|DomainSingleEntityAggregate|单实体聚合" enigma-ddd enigma-demo enigma-test-spring-boot-starter docs/dev docs/domain-driven-design openspec CHANGELOG.md agents.md`，确认无残留引用；执行 `cd enigma-parent && mvn install -DskipTests -Djacoco.skip=true` 安装本地依赖并完成全仓库编译；执行 `cd enigma-ddd && ./mvnw test`，结果 `BUILD SUCCESS`，`enigma-ddd-core` 65 个测试通过，`enigma-ddd-spring-boot-starter` 47 个测试通过。
