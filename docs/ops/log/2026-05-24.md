## 10:33

- 改动范围：`enigma-ddd` 聚合根契约及测试示例、`enigma-demo` 与 `enigma-test-spring-boot-starter` 示例聚合根、`docs/dev/convention/java-ddd-convention.md`、`docs/domain-driven-design/`、`openspec/changes/add-domain-single-entity-aggregate/`。
- 改动摘要：移除独立的单实体聚合概念与 `SingleEntityAggregate` 契约；删除对应 OpenSpec 变更、领域设计 Markdown/HTML 和专用测试类；当聚合边界内只有一个实体时，统一表达为该实体直接实现 `AggregateRoot<IdentityType>`。
- 验证方式：执行 `rg -n "SingleEntityAggregate|DomainSingleEntityAggregate|单实体聚合" enigma-ddd enigma-demo enigma-test-spring-boot-starter docs/dev docs/domain-driven-design openspec CHANGELOG.md agents.md`，确认无残留引用；执行 `cd enigma-parent && mvn install -DskipTests -Djacoco.skip=true` 安装本地依赖并完成全仓库编译；执行 `cd enigma-ddd && ./mvnw test`，结果 `BUILD SUCCESS`，`enigma-ddd-core` 65 个测试通过，`enigma-ddd-spring-boot-starter` 47 个测试通过。

## 10:56

- 改动范围：`docs/requirements/enigma-demo-prd.md`、`docs/ops/log/2026-05-24.md`。
- 改动摘要：新增 `enigma-demo` PRD 大纲，明确其作为 Enigma 标准 DDD 示例工程的产品定位、目标用户、产品目标、非目标、候选业务方向、领域建模要求、框架能力展示范围、测试验收标准和待确认问题。
- 验证方式：文档大纲创建，未运行构建测试。

## 10:58

- 改动范围：`docs/requirements/enigma-demo/prd.md`、`docs/ops/log/2026-05-24.md`。
- 改动摘要：新建 `docs/requirements/enigma-demo/` 目录，并将 `enigma-demo` PRD 大纲移动到该目录下，作为后续 `enigma-demo` 相关需求文档的集中存放位置。
- 验证方式：文档路径调整，未运行构建测试。

## 11:00

- 改动范围：`docs/requirements/enigma-demo/prd.md`、`docs/ops/log/2026-05-24.md`。
- 改动摘要：评审并精简 `enigma-demo` PRD 大纲，移除头脑风暴候选项、OpenSpec 过程性说明、过细接口字段清单和无关框架展示项，保留当前阶段已确定的产品定位、目标、边界、建模要求、展示范围、验收标准和关键待确认问题。
- 验证方式：文档内容调整，未运行构建测试。

## 11:05

- 改动范围：`docs/requirements/enigma-demo/prd.md`、`docs/ops/log/2026-05-24.md`。
- 改动摘要：用业务语言更新 `enigma-demo` 示例业务方向和核心业务场景，明确以《零之轨迹》《碧之轨迹》的克洛斯贝尔自治州为背景，选取“特别任务支援科处理支援请求”作为业务样例，并补充发布、查询、受理、完成、关闭支援请求的基础流程与规则。
- 验证方式：文档内容调整，未运行构建测试。

## 11:09

- 改动范围：`docs/requirements/enigma-demo/prd.md`、`docs/ops/log/2026-05-24.md`。
- 改动摘要：将 `enigma-demo` 示例业务方向调整为“特别任务支援科成员养成与任务准备”，第一阶段核心业务场景调整为角色状态管理，覆盖角色状态、装备栏和导力器配置，并补充创建角色、查看状态、调整状态、更换装备、配置导力器的基础流程与规则。
- 验证方式：文档内容调整，未运行构建测试。

## 11:12

- 改动范围：`docs/requirements/enigma-demo/prd.md`、`docs/ops/log/2026-05-24.md`。
- 改动摘要：补充 `enigma-demo` 第一阶段功能需求，覆盖创建角色、查看角色状态、调整角色状态、更换装备、配置导力器和异常操作处理。
- 验证方式：文档内容调整，未运行构建测试。

## 11:15

- 改动范围：`docs/requirements/enigma-demo/prd.md`、`docs/ops/log/2026-05-24.md`。
- 改动摘要：移除 PRD 中暂不需要的接口需求章节，以及 Repository、领域事件等实现侧内容；将待确认问题收敛为角色状态、导力器槽位和饰品栏位等产品需求问题。
- 验证方式：文档内容调整，未运行构建测试。

## 11:17

- 改动范围：`docs/requirements/enigma-demo/prd.md`、`docs/ops/log/2026-05-24.md`。
- 改动摘要：继续将 PRD 产品化，移除用户接口、资源库、应用服务、DDD 分层等实现侧措辞，将建模要求、展示范围和验收标准改为角色状态管理相关的业务表达。
- 验证方式：文档内容调整，未运行构建测试。

## 11:21

- 改动范围：`docs/requirements/enigma-demo/prd.md`、`docs/ops/log/2026-05-24.md`。
- 改动摘要：细化角色初始化状态，将生命值和能量值扩展为 HP、MP、CP，以及 STR、DEF、AGL、ATS、ADF、SPD、DEX、MOV、RNG 等基础战斗属性，并同步更新查看、调整和异常校验规则。
- 验证方式：文档内容调整，未运行构建测试。

## 11:24

- 改动范围：`docs/requirements/enigma-demo/prd.md`、`docs/ops/log/2026-05-24.md`。
- 改动摘要：明确导力器第一版槽位规则为 1 个核心回路槽位和 7 个普通回路槽位，并移除对应待确认问题。
- 验证方式：文档内容调整，未运行构建测试。

## 11:25

- 改动范围：`docs/requirements/enigma-demo/prd.md`、`docs/ops/log/2026-05-24.md`。
- 改动摘要：明确装备栏第一版包含两个饰品栏位，并移除已清空的待确认问题章节。
- 验证方式：文档内容调整，未运行构建测试。

## 22:08

- 改动范围：`docs/requirements/enigma-demo/strategy-design/`、`docs/ops/log/2026-05-24.md`。
- 改动摘要：基于 `docs/requirements/enigma-demo/prd.md` 生成 `enigma-demo` DDD 战略设计文档，覆盖产品愿景、业务场景、核心业务链路、子域划分、限界上下文、上下文映射、统一语言和上下文间集成策略。
- 验证方式：文档生成与路径校验，未运行构建测试。

## 22:14

- 改动范围：`enigma-parent/pom.xml`、`docs/ops/log/2026-05-24.md`。
- 改动摘要：从 `enigma-parent` 默认 Maven reactor 的 `<modules>` 中移除 `../enigma-demo`，使 `cd enigma-parent && mvn install` 默认不再构建和安装 `enigma-demo`。
- 验证方式：执行 `cd enigma-parent && mvn -q -DskipTests validate` 校验 Maven 结构。
