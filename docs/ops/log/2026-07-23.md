# 2026-07-23 操作日志

## 版本发布路线图

- 时间：2026-07-23
- 改动范围：`docs/ops/deployment/`
- 改动摘要：
  - 新增 Enigma `2.0.0` 版本发布路线图。
  - 明确 `dev/2.0.0`、`develop-2.x.x`、`release/v2.0.0` 和 `main` 的职责及发布路径。
  - 补充开发整理、集成验证、候选发布、正式发布和发布后收尾的准入与退出条件。
  - 增加代码构建、版本文档、制品发布和回滚验证清单。
- 验证方式：
  - 对照当前 Maven 模块版本、Git 分支状态、`CHANGELOG.md` 和项目发布规范进行内容核对。
  - 执行 Markdown 格式及 Git diff 检查。

## dev/2.0.0 squash 合并

- 时间：2026-07-23
- 改动范围：`dev/2.0.0`、`develop-2.x.x`
- 改动摘要：
  - 确认 `dev/2.0.0` 基于当前 `develop-2.x.x`，领先 29 个提交且不存在双向分叉。
  - 将 29 个提交的最终内容 squash 为单一提交 `e6736de`。
  - squash 提交涉及 279 个文件，包含 6975 行新增和 1330 行删除。
  - 提交后保留原有未提交内容的命名 stash，并将 squash 提交推送到远端 `develop-2.x.x`。
- 验证方式：
  - squash 暂存树及最终提交树均与 `origin/dev/2.0.0` 的树 SHA `19889c1` 一致。
  - 使用 Temurin Java `1.8.0_482` 和 Maven Wrapper 3.5.4 执行 `clean verify`。
  - Maven Reactor 10 个模块全部构建成功，测试及 JaCoCo 覆盖率检查通过。
- 已知事项：
  - 源分支存在部分 EOF 空行格式警告；为确保 squash 内容与源分支严格一致，本次未额外修改。

## Release 分支发布规范

- 时间：2026-07-23
- 改动范围：`agents.md`
- 改动摘要：
  - 新增 release 分支发布准备流程。
  - 规定正式版本使用无后缀语义化版本号，例如 `2.0.0`。
  - 规定更新 `CHANGELOG.md` 最新版本详情和版本索引。
  - 规定开发日志和操作日志按版本合并到 `summary/`，验证完整后删除已归档的源 Markdown 日志。
  - 明确 `CHANGE_LOG.md`、`dev/logs`、`ops/logs` 与仓库真实文件和目录的映射规则，避免创建重复路径。
  - 补充日志删除边界、全量构建和 Git 范围检查要求。
- 验证方式：
  - 对照当前 release 分支、现有 `CHANGELOG.md`、`docs/ops/log/` 和版本脚本检查规则中的路径与命令。
  - 执行 Markdown 格式检查和 Git diff 检查。

## Roadmap 正式版本表述

- 时间：2026-07-23
- 改动范围：`docs/ops/deployment/roadmap.md`
- 改动摘要：
  - 将目标正式版本从 `2.0.0-RELEASE` 统一调整为无后缀的 `2.0.0`。
  - 同步更新发布路径、候选发布切换条件、正式发布检查项和发布决策记录。
  - 同步当前事实：`release/v2.0.0` 已创建，各模块当前版本为 `2.0.0-SNAPSHOT`。
- 验证方式：
  - 检查 roadmap 中不存在 `2.0.0-RELEASE` 遗留表述。
  - 执行 Markdown 格式检查和 Git diff 检查。
