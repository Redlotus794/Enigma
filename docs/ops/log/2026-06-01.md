## 14:04

- 改动范围：`enigma-common/pom.xml`、`enigma-random-spring-boot-starter/pom.xml`、`enigma-demo/pom.xml`、`docs/ops/log/2026-06-01.md`。
- 改动摘要：将 Enigma 项目中尚未声明 optional 的 Lombok 模块依赖补充 `<optional>true</optional>`，避免第三方项目引用 Enigma 模块时传递加载 Lombok；`enigma-bom` 仅保留版本管理，不声明 optional。
- 验证方式：执行 `rg -n -C 3 "<artifactId>lombok</artifactId>" .. -g 'pom.xml'` 检查 Lombok 声明位置；执行 `cd enigma-parent && ./mvnw -q -DskipTests validate`，命令通过。

## 14:58

- 改动范围：`enigma-ddd/enigma-ddd-spring-boot-starter/src/main/java/io/github/redlotus794/enigma/ddd/spring/event/DefaultTransactionalEventProxy.java`、`enigma-ddd/enigma-ddd-spring-boot-starter/src/main/java/io/github/redlotus794/enigma/ddd/spring/autoconfigure/EnigmaDomainComponentAutoConfiguration.java`。
- 改动摘要：为默认事务事件代理补充 `INSTANCE` 单例，修复自动装配返回 `DefaultTransactionalEventProxy.INSTANCE` 时的编译缺口；清理自动配置类中的通配符导入与无用导入。
- 验证方式：执行 `cd enigma-parent && ./mvnw -pl ../enigma-ddd/enigma-ddd-spring-boot-starter -am test -DskipTests`，命令通过。

## 15:08

- 改动范围：`enigma-ddd/enigma-ddd-spring-boot-starter/src/test/java/io/github/redlotus794/enigma/ddd/spring/event/`、`enigma-ddd/enigma-ddd-spring-boot-starter/src/test/java/io/github/redlotus794/enigma/ddd/spring/autoconfigure/EnigmaDomainComponentAutoConfigurationTest.java`、`docs/ops/log/2026-06-01.md`。
- 改动摘要：补充准实时事件相关测试，覆盖 `RealTimeExecutionEvent` 对象契约、`DefaultTransactionalEventProxy` 执行行为、`RealTimeExecutionEventListener` 委托行为、通过 `DomainEventPublisher` 发布后的 fallback 执行链路，以及自动配置中新事务事件代理和监听器 Bean 的创建。
- 验证方式：执行 `cd enigma-parent && ./mvnw -pl ../enigma-ddd/enigma-ddd-spring-boot-starter -am test`，命令通过。

## 15:15

- 改动范围：`README.md`、`enigma-ddd/README.md`、`docs/ops/log/2026-06-01.md`。
- 改动摘要：在根 README 的 DDD 模块索引中补充准实时事件能力说明；在 DDD 模块 README 中新增准实时事件章节，说明执行机制、事务阶段、适用场景、使用示例和自定义 `TransactionalEventProxy` 的方式。
- 验证方式：执行 `rg -n "准实时事件|RealTimeExecutionEvent|TransactionalEventProxy" README.md enigma-ddd/README.md docs/ops/log/2026-06-01.md`，确认文档内容已补充。
