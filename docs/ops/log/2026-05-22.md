# 2026-05-22 操作日志

## 09:37

- 改动范围：`agents.md`
- 改动摘要：将领域设计基本概念事实来源、文档平台和维护入口统一调整为 `docs/domain-driven-design/`，移除飞书作为 DDD 概念维护来源；明确代码规范唯一来源为 `docs/dev/convention/`，未覆盖的代码规范主题不得回退到其他事实来源。
- 验证方式：使用 `rg` 检查 `agents.md` 中已无 `飞书`、`feishu` 或原飞书 wiki 链接残留。

## 09:40

- 改动范围：`agents.md`、`docs/ops/log/2026-05-22.md`
- 改动摘要：新增操作日志规则，要求每一次有意义的代码、配置、文档、规范或架构改动后记录到 `docs/ops/log/`；补充今日已完成的文档事实来源调整日志。
- 验证方式：文档规则变更，未运行构建测试。

## 09:41

- 改动范围：`agents.md`、`docs/ops/log/2026-05-22.md`
- 改动摘要：简化目录结构说明，去掉具体项目文件、模块内部文件、Java 包和类清单，仅保留主要目录与模块级职责。
- 验证方式：文档结构调整，未运行构建测试。

## 09:41

- 改动范围：`openspec/config.yaml`、`docs/ops/log/2026-05-22.md`
- 改动摘要：使用 `enigma-openspec` 规范重新生成 OpenSpec 配置，将配置改为中文上下文，统一事实来源为 `docs/dev/convention/`、`docs/domain-driven-design/`、POM、README、CHANGELOG 和 `agents.md`，并加入操作日志要求。
- 验证方式：执行 `ruby -e 'require "yaml"; YAML.load_file("openspec/config.yaml"); puts "ok"'` 通过；文档配置变更，未运行构建测试。

## 10:15

- 改动范围：`enigma-ddd/enigma-ddd-core`、`docs/dev/convention/java-ddd-convention.md`、`openspec/changes/add-domain-single-entity-aggregate/`、`docs/ops/log/2026-05-22.md`
- 改动摘要：补充 DDD Core 代码规范；新增 `DomainSingleEntityAggregate` 单实体聚合接口，默认 `root()` 返回实体自身；补充测试用单实体聚合类和单元测试；新增对应 OpenSpec proposal、spec 和 tasks。
- 验证方式：执行 `cd enigma-ddd/enigma-ddd-core && mvn test` 通过，测试结果为 `Tests run: 62, Failures: 0, Errors: 0, Skipped: 0`。

## 10:35

- 改动范围：各模块 `pom.xml`、`CHANGELOG.md`、`agents.md`、`openspec/config.yaml`、模块 README、`docs/ops/log/2026-05-22.md`
- 改动摘要：将 Enigma 项目版本统一调整为 `2.0.0-SNAPSHOT`，同步 `enigma-ddd-core` 中 import 的 `enigma-bom` 版本；将 `CHANGELOG.md` 的 `1.2.0` 条目直接调整为 `2.0.0`，并补充“资源库需要基于聚合根实现”的版本说明；为 `enigma-parent` 补充 `enigma-bom` 的本地 `relativePath`。
- 验证方式：已执行旧版本号搜索，确认无 `1.2.0` 或 `1.0.0-SNAPSHOT` 残留；执行 `cd enigma-parent && mvn validate -DskipTests` 通过，所有 reactor 模块解析为 `2.0.0-SNAPSHOT`。Maven 提示 `enigma-bom` 中 `compile-testing` 存在重复 dependencyManagement 声明，未在本次版本调整中处理。

## 10:59

- 改动范围：`enigma-bom/pom.xml`、`docs/ops/log/2026-05-22.md`
- 改动摘要：删除 `enigma-bom` 中重复的 `com.google.testing.compile:compile-testing` dependencyManagement 声明，保留 `test related` 分组下的版本管理。
- 验证方式：执行 `rg -n "compile-testing|google.testing.compile" enigma-bom/pom.xml` 确认仅剩一处声明；执行 `cd enigma-parent && mvn validate -DskipTests` 通过，Maven 不再输出重复 dependencyManagement warning。

## 15:14

- 改动范围：`enigma-ddd/enigma-ddd-core/src/main/java/com/rdlts/enigma/ddd/core/DomainAggregate.java`、`docs/ops/log/2026-05-22.md`
- 改动摘要：将 `DomainAggregate` JavaDoc 中指向领域设计文档的绝对 `file://` 链接改为仓库内相对路径，避免绑定本机绝对路径导致浏览器无法按仓库 Markdown 预览。
- 验证方式：文档链接调整，未运行构建测试。

## 15:15

- 改动范围：`enigma-ddd/enigma-ddd-core/src/main/java/com/rdlts/enigma/ddd/core/DomainAggregate.java`、`docs/ops/log/2026-05-22.md`
- 改动摘要：按要求回滚上一处 `DomainAggregate` JavaDoc 链接调整，恢复为原绝对 `file://` 链接。
- 验证方式：文档链接回滚，未运行构建测试。

## 15:20

- 改动范围：`docs/domain-driven-design/html/聚合.html`、`docs/ops/log/2026-05-22.md`
- 改动摘要：将 `docs/domain-driven-design/聚合.md` 转换为静态 HTML，生成到 `docs/domain-driven-design/html/聚合.html`。
- 验证方式：检查生成文件存在并查看 HTML 头部与正文片段；文档生成，未运行构建测试。

## 15:28

- 改动范围：`docs/domain-driven-design/html/`、`agents.md`、`docs/ops/log/2026-05-22.md`
- 改动摘要：将 `docs/domain-driven-design/` 下所有 Markdown 文档批量同步为 `html/` 下同名 HTML 文件；在 `agents.md` 中新增领域设计文档同步规则，要求 Markdown 源文档与 HTML 预览文件同步维护。
- 验证方式：检查批量生成输出，确认已生成 `agents.html`、`值对象.html`、`单实体聚合.html`、`实体.html`、`战术设计.html`、`聚合.html`、`聚合根.html`、`限界上下文.html`、`集成方式.html`；文档生成，未运行构建测试。

## 15:31

- 改动范围：`enigma-ddd/enigma-ddd-core/src/main/java/com/rdlts/enigma/ddd/core/DomainEntity.java`、`DomainAggregateRoot.java`、`ValueObject.java`、`DomainSingleEntityAggregate.java`、`docs/ops/log/2026-05-22.md`
- 改动摘要：参照 `DomainAggregate` 的 JavaDoc 链接格式，将已存在 HTML 预览的核心概念类链接统一指向 `docs/domain-driven-design/html/` 下对应文件。
- 验证方式：文档链接调整，未运行构建测试。

## 15:40

- 改动范围：各模块 `pom.xml`、模块 README、`agents.md`、`openspec/config.yaml`、`docs/ops/log/2026-05-22.md`
- 改动摘要：将 Enigma Maven 坐标 `groupId` 从 `com.rdlts.enigma` 调整为 `io.github.redlotus794`，同步更新 BOM、Parent、子模块依赖声明与文档中的 Maven 依赖示例；Java 包名仍保持 `com.rdlts.enigma`。
- 验证方式：执行 `rg -n "<groupId>com\\.rdlts\\.enigma</groupId>|GroupId.*com\\.rdlts\\.enigma" -g 'pom.xml' -g '*.md' -g '*.yaml' -g '*.yml'` 确认 Maven 坐标无旧 groupId 残留；执行 `cd enigma-parent && mvn validate -DskipTests` 通过，reactor 中所有 Enigma 模块均解析为 `io.github.redlotus794:*:2.0.0-SNAPSHOT`。

## 15:46

- 改动范围：`enigma-parent/pom.xml`、`docs/ops/log/2026-05-22.md`
- 改动摘要：在 Parent POM 中补充项目主页 URL、开发者信息和 SCM 信息；license 按项目当前 `LICENSE`、`README.md` 与 `agents.md` 要求配置为 `GNU General Public License v3.0`。
- 验证方式：执行 `cd enigma-parent && mvn validate -DskipTests` 通过，POM 结构和 reactor 解析正常。

## 15:51

- 改动范围：各模块 `src/main/java`、`src/test/java`、`META-INF/services`、`spring.factories`、模块 README、`agents.md`、`docs/ops/log/2026-05-22.md`
- 改动摘要：将 Java 包名前缀从 `com.rdlts` 统一迁移为 `io.github.redlotus794`，同步移动源码目录到 `io/github/redlotus794`，并更新 Spring 自动装配、组件扫描、SPI 服务文件名与文件内容、测试配置和文档示例；同时修正 `DomainServiceUtils`、`CachedDomainRepository`、`InMemoryDomainRepository` 中资源库泛型顺序以匹配当前 `DomainRepository<DA, DAR, IdentityType>` 定义。
- 验证方式：执行 `rg -n "com\\.rdlts" --glob '!docs/ops/log/**' --glob '!**/target/**'` 确认源码、资源与文档无旧包名前缀残留；执行 `find . -path '*/src/*/java/com' -type d` 确认旧 Java 包目录已清理；执行 `cd enigma-parent && mvn test -DskipTests -Djacoco.skip=true` 通过，全 reactor main/test 编译成功；执行 `cd enigma-parent && mvn test -Djacoco.skip=true` 时 `enigma-common` 2 个测试、`enigma-ddd-core` 61 个测试通过，随后 `enigma-test-spring-boot-starter` 仍因 BDD Spring Boot 测试启动 Tomcat `port 0` 失败中断，此为既有测试环境问题。

## 16:00

- 改动范围：`enigma-ddd/enigma-ddd-core/src/test/java/io/github/redlotus794/enigma/ddd/core/DomainAggregateRootTest.java`、`docs/ops/log/2026-05-22.md`
- 改动摘要：新增 `DomainAggregateRoot` 单元测试，覆盖默认 `nextIdentity()` 未实现时抛出 `UnsupportedOperationException` 的契约。
- 验证方式：执行 `cd enigma-parent && ./mvnw -pl io.github.redlotus794:enigma-ddd-core -am test` 通过，测试结果为 `Tests run: 62, Failures: 0, Errors: 0, Skipped: 0`。

## 16:01

- 改动范围：`enigma-ddd/enigma-ddd-core/src/test/java/io/github/redlotus794/enigma/ddd/core/service/DomainServiceRegistryTest.java`、`docs/ops/log/2026-05-22.md`
- 改动摘要：为 `DomainServiceRegistryHolder` 的默认构造函数补充单元测试覆盖。
- 验证方式：执行 `cd enigma-parent && ./mvnw -pl io.github.redlotus794:enigma-ddd-core -am test` 通过，测试结果为 `Tests run: 63, Failures: 0, Errors: 0, Skipped: 0`。

## 16:02

- 改动范围：`enigma-ddd/enigma-ddd-core/src/test/java/io/github/redlotus794/enigma/ddd/core/event/DomainEventPublisherTest.java`、`docs/ops/log/2026-05-22.md`
- 改动摘要：为 `DomainEventPublisherHolder` 的默认构造函数补充单元测试覆盖。
- 验证方式：执行 `cd enigma-parent && ./mvnw -pl io.github.redlotus794:enigma-ddd-core -am test` 通过，测试结果为 `Tests run: 64, Failures: 0, Errors: 0, Skipped: 0`。
